# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Override files (local only, not to be shared)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform variable files with secrets
*.tfvars
*.tfvars.json

# Terraform lock file (optional to ignore, sometimes kept)
.terraform.lock.hcl
